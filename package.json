{
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "test:packages": "lerna run test --scope \"+(core|app)\"",
    "prepare": "lerna run prepare",
    "publish-by-lerna": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^2.8.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
